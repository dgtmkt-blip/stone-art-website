# Supabase project used for the enquiry/booking/contact forms.
# The anon/publishable key is safe to expose client-side — RLS on the
# `inquiries` table restricts it to insert-only.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
